# 领域工具清单（Agent Tools）

> 状态：v1.0（2026-06，M2-T08 交付）
> 代码：`server/sjherp-app/src/main/java/com/sjherp/app/tool/`（按域分包：catalog / partner / warehouse）
> 装配：`DomainToolConfig`（常驻注册，所有 profile 生效）；演示工具仍在 `ToolConfig.DemoToolConfig`（仅 dev/local）
> 关联：[Agent执行循环.md](./Agent执行循环.md)（高风险拦截 §4、安全壳 §5）、[流程缺口通道.md](./流程缺口通道.md)（record_process_gap）

## 1. 设计约定

- **工具即领域服务**（CLAUDE.md 原则 1）：所有工具执行一律经各 domain Service 唯一入口，绝不绕过；
- **风险等级**：查询类 NORMAL（直接执行）；创建类 HIGH（档案创建影响主数据，框架强制确认卡片，
  用户点「确认执行」后才真正执行——提示词只要求模型复述要点后直接调用，避免双重确认）；
- **审计操作人**：写操作 created_by/updated_by 记 `agent:<userId>`（userId 为会话所属 sys_user.id，
  区分人工与 Agent 操作，公共助手 `ArchiveToolSupport.operator`）；
- **查询返回上限**：精简列表最多 10 条（`ArchiveToolSupport.MAX_ITEMS`），超量时附 note 引导用户缩小范围；
- **编码自动生成**：创建类工具一律不接收编码参数，由领域服务按编号规则生成（CUS/SUP/SKU/WH-年月-序号）；
- **权限点**：创建类工具声明的 `requiredPermission` 已真实生效（M2-T06）：
  `RolePermissionToolChecker` 按会话所属用户实时查角色 → [权限矩阵](./权限矩阵.md) 判定；
  权限不足时工具不执行，"权限不足"错误回灌给模型向用户说明。查询类工具无权限点（登录即可）。

## 2. 工具一览

### 查询类（riskLevel = NORMAL，无确认）

| 工具名 | 参数（* 必填） | 返回 | 对应 Service |
|---|---|---|---|
| `search_products` | keyword（模糊匹配编码/名称/条码）、status（ENABLED/DISABLED） | 最多 10 条：id/code/name/spec/baseUnit/status + total | `ProductService.search`（单位名经 `UnitService.findAll`） |
| `get_product_detail` | id 或 code（至少一个，同传以 id 为准） | 完整档案：含条码/备注/多单位换算（rate 字符串承载 + meaning 文案"1 箱 = 12 瓶"） | `ProductService.get` / `search`（编码精确匹配） |
| `search_customers` | keyword（编码/名称/联系人/电话）、status | 最多 10 条：id/code/name/contactPerson/contactPhone/settlementMethod/status + total | `CustomerService.search` |
| `search_suppliers` | keyword（同上）、status | 同上 | `SupplierService.search` |
| `search_warehouses` | keyword（编码/名称/负责人）、status | 最多 10 条：id/code/name/address/manager/status + total | `WarehouseService.search` |
| `query_inventory_balance` | warehouse*（名称或编码）、product*（名称或编码） | 结存数量/金额/派生加权单价（字符串承载）；名称解析歧义/未命中时返回候选清单 | `TransactionalInventoryService.balanceOf`（M3-T01c；解析经 Warehouse/ProductService.search） |

### 创建类（riskLevel = HIGH，框架确认卡片后执行）

权限点已生效（M2-T06），持有角色见 [权限矩阵](./权限矩阵.md)：

| 工具名 | 参数（* 必填） | 权限点（持有角色） | 对应 Service |
|---|---|---|---|
| `create_customer` | name*、contact_person、contact_phone、address、tax_no、settlement_method（MONTHLY 默认/CASH/PREPAID） | partner:create_customer（ADMIN/BOSS/SALES） | `CustomerService.create`（信用额度本批不开放，界面维护） |
| `create_supplier` | 同 create_customer（无信用额度概念） | partner:create_supplier（ADMIN/BOSS/PURCHASER） | `SupplierService.create` |
| `create_product` | name*、base_unit*（单位**名称**，须已存在）、spec、barcode、remark | catalog:create_product（ADMIN/BOSS） | `ProductService.create`（类目/多单位换算本批不开放） |
| `create_warehouse` | name*、address、manager | warehouse:create_warehouse（ADMIN/BOSS/WAREHOUSE） | `WarehouseService.create`（库位开关本批不开放） |
| `adjust_inventory` | type*（OPENING/COST_ADJUST）、warehouse*、product*（名称或编码）、quantity/unit_cost（OPENING 必填）、adjust_amount（COST_ADJUST 必填，可负）；数值一律字符串承载 | inventory:adjust（ADMIN/BOSS/WAREHOUSE） | `InventoryAdjustmentService`（M3-T01c：OP-/CA- 自动编号 → TransactionalInventoryService 唯一写入口；仅期初与成本调整，日常出入库走业务单据） |

### 通用（M1-T04 起常驻）

| 工具名 | 风险 | 说明 |
|---|---|---|
| `record_process_gap` | NORMAL | 流程缺口记录，见 [流程缺口通道.md](./流程缺口通道.md) |

## 3. create_product 的单位解析约定

基本单位按**名称**解析（用户在聊天里说"单位是把"，模型无从得知单位 id）：

- 名称在 `UnitService.findAll()` 中精确匹配（strip 后全等）；
- 不存在时返回清晰错误：点名不存在的单位 + 列出系统已有单位 + 引导话术
  （改用已有单位，或先在界面「基础档案-计量单位」创建——本批**没有** create_unit 工具，
  提示词明确要求模型不假装能创建单位）；
- 注意时序：HIGH 拦截发生在执行前，因此"单位不存在"要等用户**确认后**才暴露
  （工具执行失败 → 错误回灌 → 模型向用户解释并引导）。实测（2026-06，DeepSeek）模型
  能正确转述错误并给出两个引导选项。

## 4. 系统提示词约定（LlmAgent「当前业务能力」段）

- 查询直接调用，引用真实数据回答；
- 创建类：先在 text 复述要点 → 直接发起调用 → 框架自动出确认卡片，
  模型**不得**自己再造一轮"是否确认"问答（避免双重确认），也不得在未执行成功前声称已创建；
- 工具未覆盖的需求走流程缺口通道（record_process_gap）。

## 5. 测试

`sjherp-app` 的 `app/tool` 测试（mock 各 domain Service + 真实 `JsonSchemaToolArgumentValidator`）：
查询条件映射（固定 page=1/size=10）、精简字段与中文标签、创建命令映射、结算方式默认 MONTHLY、
操作人 `agent:<userId>`、领域校验拒绝转 fail 回灌、create_product 单位解析失败路径（不触碰 Service）、
schema 必填/枚举/多余字段校验。

## 6. 已知边界与后续

- 更新/启停档案暂未开放工具（本批只做查与建；改档案走界面，后续里程碑评估）；
- 单位/类目的创建未开放工具（领域上单位精度影响出入库舍入，需引导用户在界面确认精度）；
- ~~创建类工具的权限点等待 M2-T06 真实权限模型接入后生效~~（已生效，2026-06 M2-T06：
  RolePermissionToolChecker 替换 allowAll 占位，矩阵见 [权限矩阵.md](./权限矩阵.md)）。

## 7. M3 进销存工具（T03/T04/T05/T08/T11 + T13，2026-06）

> 业务视角的全链路说明与对话脚本见 [业务-进销存Agent工具.md](./业务-进销存Agent工具.md)。
> 装配统一在 `DomainToolConfig`（常驻）；HIGH 工具的权限点反漂移由 `HighRiskToolPermissionTest` 守门（常驻基线 40 + 演示 2 = 42）。

### 库存受控单据（M3-T03 盘点 / T04 调拨）

| 工具名 | 风险 | 权限点 | 对应 Service |
|---|---|---|---|
| `create_stock_count` / `query_stock_count` | HIGH / NORMAL\* | inventory:count | `StocktakeService` |
| `create_transfer` / `query_transfer` | HIGH / NORMAL\* | inventory:transfer | `TransferAppService` |

\* 盘点/调拨查询接口因属受控动作也要求对应权限点（见权限矩阵），区别于「查询登录即可」通则。

### 采购线（M3-T05 订单 / T11 收货·发票·应付）

| 工具名 | 风险 | 权限点 | 对应 AppService |
|---|---|---|---|
| `create_purchase_order` / `approve_purchase_order` / `query_purchase_order` | HIGH/HIGH/NORMAL | purchase:order | `PurchaseOrderAppService` |
| `create_purchase_receipt` / `approve_purchase_receipt` / `post_purchase_receipt` / `query_purchase_receipt` | HIGH×3 / NORMAL | purchase:receipt | `PurchaseReceiptAppService`（仓库名解析经 `WarehouseService`）|
| `create_purchase_invoice` / `approve_purchase_invoice` / `post_purchase_invoice` / `query_purchase_invoice` | HIGH×3 / NORMAL | purchase:invoice | `PurchaseInvoiceAppService` |
| `query_payables` | NORMAL | 无（登录即可）| `PurchaseInvoiceAppService.searchPayables` |

### 销售线（M3-T08 订单 / T11 出库·发票·应收）

| 工具名 | 风险 | 权限点 | 对应 AppService |
|---|---|---|---|
| `create_sales_order` / `approve_sales_order` / `query_sales_order` | HIGH/HIGH/NORMAL | sales:order | `SalesOrderAppService` |
| `create_sales_delivery` / `approve_sales_delivery` / `post_sales_delivery` / `query_sales_delivery` | HIGH×3 / NORMAL | sales:delivery | `SalesDeliveryAppService`（仓库/商品名解析）|
| `create_sales_invoice` / `approve_sales_invoice` / `post_sales_invoice` / `query_sales_invoice` | HIGH×3 / NORMAL | sales:invoice | `SalesInvoiceAppService`（商品名解析）|
| `query_receivables` | NORMAL | 无（登录即可）| `ReceivableAppService.search` |

### 数据一致性校验（M3-T13）

| 工具名 | 风险 | 权限点 | 对应 Service |
|---|---|---|---|
| `run_consistency_check` | NORMAL | 无（登录即可）| `ConsistencyCheckService`（只读勾稽报告，见 [业务-数据一致性校验.md](./业务-数据一致性校验.md)）|

> 设计要点（M3-T11）：建单/审核(approve)/过账(post) 各为独立 HIGH 工具，忠于状态机 DRAFT→APPROVED→COMPLETED 与职责分离；过账才落库存流水/应付应收，由 AppService 外层 `@Transactional` 保证「状态变更 + 库存过账 + 应付应收生成」原子。多行参数用 lines 数组、小数字符串承载。
