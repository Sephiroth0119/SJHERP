# M6-T09 实施计划

1. 增加 V36 任务真源，保留 T08 候选/来源 append-only 语义。
2. 增加开发任务领域状态、幂等创建、lease CAS、失败重试和人工批准门禁。
3. 增加 fake/local runner 与管理员 REST 入口；默认关闭真实外部副作用。
4. 覆盖权限、状态机、幂等、租约 ABA、命令 allowlist、超时/输出上限和恢复测试。
5. Java 21 后端全量、前端构建、MySQL 8.4 CI 门禁，并做独立对抗复审。
