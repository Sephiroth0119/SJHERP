# M7-T01 登录与权限前端测试说明

状态：✅ 已完成（2026-07-25）。

## 范围

- 保留既有登录页，仅补 token 生命周期安全提示与失效处理。
- `POST /api/auth/login`、`GET /api/auth/me` 增加向后兼容的 `permissions` 权限码集合；集合由后端 `RolePermissions` 真源按角色并集导出。
- 前端不复制角色到权限的矩阵，只使用后端权限码集合渲染菜单和执行体验层 hash 路由守卫；后端仍是最终授权边界。
- 未知权限码、旧缓存缺少 `permissions`、非法/过期 token 均 fail-closed；不实现 M7-T02 业务页面或 M7-T03 SSE。

## 自动化覆盖

- 后端 `RolePermissionsTest`：多角色权限码并集、稳定排序和不越权断言。
- 后端 `AuthControllerTest`：登录与 `/me` 均返回权限数组，SALES 包含 `sales:order` 且不包含 `memory:manage`。
- 前端 Node tests：权限菜单过滤、角色标签不产生授权、未知权限安全降级、旧用户缓存归一化、过期 token 清理、临期 token 提示、直接 URL 解析与未授权路由回退。
- CI 前端 job：`npm test`、`npm run build`；本地另执行 `git diff --check`。

## 证据

- Java 21：`C:\Users\Sephiroth\.jdks\jdk21\jdk-21.0.11+10`。
- 本地定向后端测试与前端测试/build 通过；MySQL 8.4 真库由 CI `integration-db` 门禁验收。

## 最终交付证据

- PR #16（`codex/m7-t01-login-permissions` → `codex/m6-t10-closure-feedback`）最终代码提交为 `e4865517b9d2a3e31b06a01d9e6118a6aedab076`，文档收口提交随该 PR 推送。
- PR #16 的后端 `mvn verify`、前端 `tsc + build`、MySQL 8.4 Testcontainers 三项 CI 全绿，PR 状态为 OPEN/CLEAN；前端测试 14/14 通过。
- 独立复审已修复 React Hooks 早退顺序与 auth/chat 会话隔离（手动退出、401、无效/过期 token 均统一清理），最终无 P0-P2；Java 21 后端全量基线及认证/权限定向证据通过。
- MySQL 8.4 真库证据来自 CI Testcontainers；本地未虚构 Docker 执行结果。
