# M6-T10 闭环回写测试说明

状态：已完成（2026-07-20）。真实验收由 GitHub Actions MySQL 8.4 integration-db 门禁执行。

当前契约：`POST /api/developer-agent/tasks/{id}/confirm-resolution` 仅 ADMIN/BOSS 可调用；输入为受校验的 `reference`、`summary`；服务显式接收 `operator`，审计 action 为 `developer.task.confirm_resolution`，targetId 为 taskId。

已修复的持久化边界：V37 通过 tenant+id 复合外键约束 `closure_feedback` 到 `developer_agent_task` 和 `gap_issue_candidate`；task claim 与通知去重仅捕获 DuplicateKeyException，其他数据库错误继续抛出；session 无法解析时回退 reporter，仅处理 NumberFormatException。

已覆盖：未 APPROVED 拒绝、显式确认前零副作用、重复 claim 短路、权限注解、V37 迁移契约、JDBC claim 异常语义、Java21 定向测试。

已完成真实验收：`ClosureFeedbackTransactionalIntegrationTest` 通过 Spring 代理连接 MySQL 8.4，覆盖成功闭环、并发幂等、失败回滚与重试、多 session 同用户去重、缺失 session 回退 reporter、真实审计 operator/target；同时保留并通过 T03 `GAP_SOLUTION` 已登录用户召回与匿名/写权限门禁证据。本机未运行 Docker 真库验收，以上结论以远端 CI 为准。
