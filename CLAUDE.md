# SJHERP — 下一代 Agent 原生 ERP

## 项目愿景

面向**小型企业**的下一代 ERP 客户端，业务涵盖**进销存 + 生产 + 财务**。
核心差异：用户不再学习复杂的 ERP 操作，而是通过 **Agent 聊天界面**完成业务（也保留传统的业务入口直接点击）。Agent 在需要用户决策时返回**选项卡片**供点击，而不是让用户打字描述一切。

### 为什么做这件事（项目第一性原理）

传统企业很难落地 Agent ERP，根因是：数据模型破碎、垃圾数据多、流程不规范——本质上是因为旧 ERP 太复杂、上手门槛太高，导致使用者不愿意遵循规范。本项目的回答：

1. **把门槛降到聊天**，让规范流程成为最容易的路径，而不是最难的。
2. **流程缺口自进化闭环**：用户遇到系统做不到的事 → 向 Agent 提出 → Agent 单开一条线记录缺口、提交开发 issue → 开发者 Agent 识别后自动开发补齐 → 热部署上线 → 该业务知识写入**系统大记忆**，供后续统计/查询复用。
3. **永远严格保持业务数据模型不破碎**——这是全项目最高优先级的不可妥协原则。

## 不可妥协的原则（任何代码改动都不得违反）

1. **数据模型完整性高于一切**。Agent、热部署的扩展、临时需求，都不允许绕过领域模型直接写库。所有写操作必须经过领域服务层，受单据状态机和校验规则约束。宁可拒绝一个需求，不可破坏一次模型。
2. **财务数据专业性**。结算单、存货核算、库存数据等财务产出必须符合会计逻辑：借贷平衡、成本核算方法一致（移动加权/先进先出，全局统一配置）、期间不可随意重开。财务记录**只可冲销、不可物理修改/删除**。
3. **可审计**。每一笔业务数据变更必须有审计日志：谁（用户/哪个 Agent）、何时、依据什么指令、改了什么。Agent 自动执行的操作尤其要可追溯。
4. **数据一致性持续校验**。独立的**检查 Agent** 定时分析最新数据是否符合业务逻辑（库存账实、应收应付与单据勾稽、工单领料/完工与存货成本勾稽、总账与明细账平衡等），发现错误立刻上报，不静默修复。
5. **金额与数量精度**：金额一律 `BigDecimal`（数据库 `DECIMAL`），禁止 float/double 参与任何金额/数量/成本运算。

## 技术栈（已定）

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | Java 21 + Spring Boot 3.x，Maven | 业务核心 |
| Agent 框架 | **完全自研**（本项目核心资产之一） | 不引入 LangGraph/Spring AI 编排，详见下文设计要点 |
| 前端 | React + TypeScript（Vite） | 聊天界面为主入口 + 传统业务入口 |
| 业务数据库 | MySQL 8.x | 所有进销存/财务数据，InnoDB，强事务 |
| 向量库（大记忆） | 独立向量库（候选 Qdrant，轻量易部署；待定） | 系统大记忆：业务知识、流程缺口解决方案、统计口径 |
| LLM 接入 | 自建抽象层，可切换 | 业务代码只依赖统一接口；DeepSeek/通义/Claude/GPT 可配置切换；检查 Agent、开发者 Agent 等关键环节配强模型 |

## 仓库结构（规划）

```
SJHERP/
├── CLAUDE.md
├── docs/                  # 架构决策(ADR)、领域模型文档、会计核算规则
├── server/                # Java 后端（Maven 多模块）
│   ├── sjherp-agent/      # 自研 Agent 框架（领域无关，可独立演进）
│   ├── sjherp-domain/     # 领域模型与领域服务（进销存+财务核心）
│   ├── sjherp-app/        # 应用层：Agent 工具(Tool)定义、API、编排入口
│   └── sjherp-infra/      # 持久化、LLM 抽象层实现、向量库客户端
└── web/                   # React + TS 前端
```

> 注意：以上为规划。实际创建模块时遵循此结构；若需调整，先更新本文件和 docs/ 中的 ADR。

## 自研 Agent 框架设计要点

- **状态显式持久化**：Agent 会话状态存数据库，进程重启/热部署后可恢复，不依赖内存。
- **工具（Tool）即领域服务**：Agent 能做的所有业务操作都是注册的领域服务方法，带参数校验与权限。Agent 没有"裸 SQL"或自由写库的工具。
- **选项返回协议**：Agent 回复是结构化消息（文本 + 可选项数组 + 可选表单），前端将选项渲染为可点击卡片。这是前后端的核心契约，定义在 docs/ 中并保持版本化。
- **Human-in-the-loop**：涉及资金、过账、期间关账等高风险操作，Agent 必须返回确认选项，由人点击确认后才执行。
- **流程缺口通道**：Agent 判断"当前能力做不到"时，走专门的缺口记录流程（结构化记录场景、期望、缺失能力）→ 生成开发 issue → 写入大记忆。不允许 Agent 在能力不足时自由发挥绕过模型硬做。
- **检查 Agent**：独立调度（定时任务），输出结构化的不一致报告。

## 业务范围

- **进**：采购订单 → 入库 → 应付 → 付款
- **销**：销售订单 → 出库 → 应收 → 收款
- **存**：库存台账、盘点、调拨、存货成本核算
- **产**（生产模块）：
  - **计划**：SOP（销售与运作计划）/ DP（需求计划）→ 依据 BOM 展开生成生产建议与采购建议
  - **执行**：WO 生产任务单（工单）→ JIT 领料/齐套检查 → 报工 → 完工入库
  - **基础数据**：BOM（物料清单）、工艺路线（简化版，小企业够用即可）
  - **成本**：工单维度归集料工费，完工结转至存货成本，与财务模块勾稽
- **财务**：总账、应收应付、结算单、期间结账、基础报表

单据皆为状态机（草稿 → 审核 → 执行 → 完成/冲销），状态流转规则定义在领域层，文档化在 docs/。

## 开发约定

- **语言**：代码标识符用英文；注释、文档、commit message、用户可见文案用中文。
- **Git**：commit message 用中文，格式 `类型: 简述`（类型：feat/fix/refactor/docs/test/chore）。主分支 `main`，功能分支 `feat/xxx`。
- **测试**：领域层（尤其库存成本核算、财务勾稽逻辑）必须有单元测试；金额计算类测试覆盖边界（四舍五入、负数、冲销）。
- **禁止事项**：
  - 不写裸 SQL 绕过领域服务做业务写操作（报表只读查询除外）。
  - 不在 Agent prompt 里硬编码业务规则——业务规则在领域层代码中，Agent 通过工具调用获得。
  - 不引入未在本文件登记的重量级框架/中间件，先讨论并记 ADR。
- **Subagent 派工模型分级**（详见路线图 §13.6）：模式复制类任务用 sonnet、复杂逻辑与交叉校验用 opus、编排与决策由主模型把控；质量闸门（测试+CI+校验）不随模型降档。
- **API 调试**：dev/local profile 下开放 API 文档（生产强制关闭）。**需鉴权的接口调试用 `/swagger-ui/index.html`**——点 Authorize 填 token 后 Execute 自动带 `Authorization: Bearer <token>`，最省事；`/doc.html`（knife4j）界面更友好可浏览，但因 knife4j 处于纯静态 UI 模式（见下），其「调试」面板的全局 Authorize **不会注入鉴权头**，需在「调试 → 请求头部」手填 `Authorization`（带不带 `Bearer ` 均可——JwtAuthenticationFilter 已容忍可选前缀，仍全量验签）。
  登记依赖：`springdoc-openapi-starter-webmvc-ui:2.7.0`（/v3/api-docs 生成 + 标准 swagger-ui，兼容 Spring 6.2.x）+ `knife4j-openapi3-ui:4.5.0`（仅前端静态资源，不引入其 autoconfigure；knife4j-openapi3-jakarta-spring-boot-starter 的 Spring autoconfigure 与 springdoc 2.4+ 二进制不兼容，故弃用——代价是 knife4j 增强特性[含调试面板全局鉴权注入、knife4j.enable 等]失效，鉴权调试以 swagger-ui 为准）。

## 当前状态

底座 v0.1 + M1/M2 主体已完成：自研 Agent 框架具备工具调用执行循环与高风险框架级确认拦截（HITL）、流程缺口记录通道、调用观测（token/耗时落库）；业务侧有商品/客户/供应商/仓库档案（领域服务+REST+Agent Tools）、通用单据状态机与编号规则、JWT 认证与角色、角色权限模型双层生效（Agent 工具 + REST，矩阵见 docs/权限矩阵.md）、统一审计切面（audit_log，人工/Agent 可区分）、会话历史滚动摘要、CI 门禁（含 Testcontainers DB 集成测试 job）。**M1 全部完成**；**M2 全部完成**（T09 期初导入已交付：POI Excel 导入档案+期初库存经唯一入口、逐行校验报错、全有或全无；期初应收应付按数据模型红线暂缓到 M4）。**M3 进销存全部完成 T01–T13**：库存线 T01-T04（台账/移动加权成本/盘点/调拨，唯一写入口+对账恒等式+并发一致性）、采购线 T05-T07（订单→入库 PURCHASE_IN→发票→应付）、销售线 T08-T10（订单→出库 SALES_OUT 含 COGS 移动加权→发票→应收）、**T11 进销存 Agent 工具全量（采购/销售各 10 个：下单/审核/收发货/过账/开票/查询，建单·审核·过账各独立 HIGH 确认卡片，常驻工具 40 个）**、**T13 数据一致性校验（ConsistencyCheckService 7 类勾稽 + REST + run_consistency_check 工具 + @Scheduled 检查 Agent + 端到端 PurchaseToSalesFlowIntegrationTest 真库验收 0 ERROR break）**、**T12 进销存报表（4 张只读报表 /api/reports/*：库存余额表/收发存汇总[tie-out 期初+收−发=期末]/采购入库明细/销售出库明细[销售额·COGS·毛利]；主表一律 LEFT JOIN 防财务报表静默丢行、金额 BigDecimal 字符串、ReportQueryIntegrationTest 真库 tie-out 验收）**。单据均走状态机+审计；应付/应收台账已生成（核销/账龄 M4-T03 已完成）；跨发票超额开票防控已修。**M4 财务核心开篇——M4-T01 总账基建已完成**（com.sjherp.domain.gl）：会计科目表（小企业会计准则预置走 V19 迁移，is_preset 守护）、记账凭证 Voucher（继承 BusinessDocument，DRAFT→APPROVED 一步过账，beforeTransition 白名单兜底"只可冲销"；**保存即校验借贷平衡 Σ借=Σ贷、不平无法保存**）、账期管理（OPEN/CLOSED，**关账期过账抛 PeriodClosedException 被拒**）、试算平衡/科目余额派生（仅统计已过账凭证，维护型余额表留 T05）；V19 四表 + /api/gl/** REST + finance:*(account/period/period_reopen/voucher) 权限（科目/账期查询登录即可、凭证全程受控）+ @Audited 全覆盖；两条验收真库 GeneralLedgerPostingIntegrationTest 通过；设计真源 docs/M4拆解-总账基建.md。**M4-T02 业务→凭证自动化已完成**：凭证模板引擎 AutoVoucherService 在 M3 四个采购/销售过账事件**同事务内自动生成并过账凭证**（入库 借1405/贷220201 暂估应付、采购发票 借220201/贷220202 正式应付、出库 借6401/贷1405 COGS、销售发票 借1122/贷6001）；**暂估入账模型**（V20 拆 2202→220201 暂估/220202 正式）解决货到票未到、使存货/应付与子账时点勾稽；**幂等**（source_doc_no 回填 + findBySourceDocNo 查重 + V20 uk_voucher_source 唯一约束）保证每单恰一组、重过账不重复；账期缺期自动 open / CLOSED 经 post 抛 PeriodClosedException 回滚；无新增权限点（自动凭证由业务过账权限驱动）；设计真源 docs/M4拆解-业务凭证自动化.md。**M4-T03 应收应付核销/账龄已完成**：核销引擎（新 com.sjherp.domain.settlement）——AccountsReceivable/Payable.settle（部分核销累加、OPEN→PARTIAL→SETTLED、超额抛 OverSettlementException 硬拒）+ **只追加** SettlementRecord（子账 settled_amount 是其维护型 rollup、记录表才是真源，原则 2/3）+ SettlementService（@Audited，装载→settle→save 子账 UPDATE→落记录）；V21 settlement_record 表（payment_doc_no 留 T04 回填/T07 红冲锚点）；JdbcAccountsPayableRepository 补核销 UPDATE 路径+findById。账龄分析（app.finance AgingReportDao 只读，按对手方 5 桶[未到期/逾期1-30/31-60/61-90/90+ CASE SUM 未核销余额]+grandTotal，**主表 LEFT JOIN 不静默丢行**）GET /api/reports/receivable-aging|payable-aging + 核销历史 GET /api/settlements；新增 finance:settlement 权限（ADMIN/BOSS/ACCOUNTANT，本批仅护只读）。**关键裁定（T03/T04/T07 切分）**：减少应收/应付与收到现金是同一经济业务（借银行/贷应收），脱离收付款的独立核销即记账错误→核销引擎本批**不出 GL、不暴露独立 settle 写入口、无生产触发器**（仅 M4-T04 收付款单同事务触发，届时配现金侧凭证+银行账户档案），冲销留 T07；故 T03 上线**无"动子账不动 GL"路径**。真库 SettlementEngineIntegrationTest（核销状态落库/记录追溯/超额拒绝 DB 态不变/findByPaymentDocNo）+ AgingReportIntegrationTest（5 桶 tie-out+边界临界 0/1/30/31/90/91+全结清剔除+档案缺失 LEFT JOIN 不丢行）验收；设计真源 docs/M4拆解-应收应付核销账龄.md。**1032 后端测试全绿**（M4-T01 三镜头 0 P0、1 P1 已修[凭证状态流转白名单]；M4-T02 三镜头 0 P0、P1 系误报、P2/P3 已修[出库凭证日改过账日 now(UTC)、账期并发 open 改回滚可重试]；M4-T03 四镜头 0 P0/P1，P2[findByPaymentDocNo 真库覆盖]+4 P3[V21 tenant_id 对齐全库 BIGINT、账龄 WHERE 加余额>0 兜底、桶边界样本、超额回滚注释据实改]已修，AP 核销 UPDATE 未带 tenant_id 登记 ADR-002 多租户统一收口）。业务文档见 docs/业务-*.md。**M4-T04 收付款单开工（L 任务拆 a/b/c）——T04a 现金/银行账户档案已完成**：照 warehouse 范式新建 com.sjherp.domain.fund（PaymentAccount，FA- 编号，glAccountCode 校验须末级+启用 GL 货币科目）+ V22 + /api/fund/accounts + finance:payment_account 权限 + 2 Agent 工具（create/search NORMAL）；**1090 后端测试全绿**；二镜头校验 0 P0/P1，1 命名 P2 已修。**T04b 收款单/付款单已完成**：com.sjherp.domain.collection(RCPT-)/payment(PAYV-) header+分摊行，过账 AppService 同一 @Transactional 内推单据状态机→逐行核销(settleReceivable/settlePayable，paymentDocNo=单号，校验对手方一致，跨对手方/超额整单回滚)→现金侧凭证(收 借 glAccountCode/贷 1122；付 借 220202/贷 glAccountCode，科目由资金账户动态传入)；AutoVoucherService +generateForCollectionReceipt/PaymentDisbursement、VoucherSourceType +2；V23/V24；REST /api/collections、/api/payments 复用 finance:settlement；**1208 后端测试全绿**；真库 CollectionPaymentFlowIntegrationTest 端到端验收；三镜头 0 P0/P1，2 P2 已修(过账校验资金账户 ENABLED 态、单内同笔 AR/AP 去重，各补单测)。设计真源 docs/M4拆解-收付款单.md。**M4-T04c 已完成**：8 个收付款 Agent HITL 工具（建·审·过账各独立 HIGH 确认+查询，finance:settlement，常驻工具 50）+ ConsistencyCheckService 核销 rollup 勾稽 3 规则（settled==Σ核销记录/settled≤amount/状态-余额一致，全 ERROR）；**M4-T04 收付款单全部完成（a 资金账户 + b 收付款单 + c 工具勾稽）——资金闭环 资金账户→收/付款单→T03 核销引擎→现金侧凭证、账实勾稽闭合**。**1280 后端测试全绿**。**M4-T05 月末结转与关账流程已完成（beta 退出标准核心）**：com.sjherp.app.gl.PeriodCloseService 在总账基建之上编排「结转前一致性闸门 → 损益结转凭证 → 试算平衡断言 → 关账」**单一 @Transactional 原子**（任一步失败整事务回滚）；**关键裁定 T05 无需建表**——损益结转是普通凭证，复用 voucher/voucher_line + createFromSource 幂等锚点（来源 PERIOD_CLOSING/源单号=账期键，命中 V20 uk_voucher_source 每账期至多一张），维护型 account_period_balance 期末冻结表随 T06 资产负债表/利润表协同设计登记延后。close 七步：①账期 OPEN 校验②重结防护 findBySourceDocNo③一致性闸门 check() 取 ERROR 阻塞[WARN 不阻塞]携 reasons④损益结转（仅 AccountType.PROFIT_LOSS 按 net=Σ借−Σ贷 方向冲平→4103 本年利润两腿各>0 才出行、Σ借==Σ贷自证、损益全无发生额则不建凭证；COST 类期末不结转留 M5）⑤试算断言 Σ借==Σ贷 且损益类净额=0⑥关账⑦返回；precheck 只读预检（向导/Agent 展示可否关账+结转预览+净利润+ERROR/WARN 清单）。PeriodCloseBlockedException→409 体含 reasons；REST close 改走 PeriodCloseService（旧 AccountingPeriodAppService.close 保留为领域原语末步调用）+ 新增 GET /api/gl/periods/{period}/close-precheck，**复用 finance:period 无新增权限点**；2 Agent 工具（precheck_period_close NORMAL / close_accounting_period HIGH HITL，常驻 50→52）。真库整月关账验收 PeriodCloseFlowIntegrationTest（整月业务→关账成功→损益归零→4103=毛利→Σ借=Σ贷→CLOSED→关账后过账被拒+回滚→重复关账拒→脏数据闸门拒账期仍 OPEN 无结转凭证→审计 period.close/voucher.post）；设计真源 docs/M4拆解-月末结转关账.md。**1335 后端测试全绿**（opus 二镜头对抗校验 0 P0/P1/P2，2 P3 已修[buildClosingPlan 4103 读取下沉、结转摘要前缀提常量解耦枚举 label]）。**M4-T06 财务报表已完成**：由 voucher/voucher_line **累计派生**资产负债表（时点 Assets=Liab+Equity）与利润表（本期+本年累计 Rev−Exp=NetProfit），小企业会计准则映射（com.sjherp.app.finance.FinancialStatementDao 只读 + FinancialStatementService + DTO + REST /api/reports/balance-sheet|income-statement）；**关键裁定 account_period_balance 期末冻结表仍不建**（修正 T05「随 T06 落地」暂定说法——已关账期不可过账→派生天然稳定，冻结表是纯性能优化非正确性所需且在最高风险关账路径加写逻辑反增故障面，登记未来优化）；**资产负债表恒平衡**靠「本期未结转损益折入未分配利润」（开账期/未关账也平衡）、contra 科目自然为减、不可分类落「其他」行不丢；**利润表排除 PERIOD_CLOSING 结转凭证**（否则损益自相抵消归零）+「其他损益（未分类）」兜底行（防新增损益科目静默漏列），**净利润==结转 netProfit==毛利**交叉校验；新增 finance:report 权限（ADMIN/BOSS/ACCOUNTANT，T08 复用）。真库整月报表对账验收 FinancialStatementFlowIntegrationTest；设计真源 docs/M4拆解-财务报表.md。**1364 后端测试全绿**（opus 二镜头 0 P0/P1，3 P2 均可演进性隐患当前无活跃缺陷——已修利润表硬编码 vs 动态口径不对称[加兜底行+单测]、测试夹具 6401/6402 误标 COST→PROFIT_LOSS，登记 COST/WIP 折入权益的 M5 限制[生产成本结转时改列在制品资产]）。**M4-T07 统一冲销机制开工（L 任务拆 a/b/c）——T07a 凭证红冲基元 + 统一防删验收已完成**：`VoucherService.reverse`——**红字=借贷对调（反向分录）不用负额**（VoucherLine 强制恰一方>0、非负→负额非法；对调使科目净额归零、Σ守恒），红字凭证在原账期(须 OPEN)过账、来源 VOUCHER_REVERSAL+原号（避 uk_voucher_source 冲突+物理幂等每原凭证至多一红冲），双向 linkage（红字 reversal_of_id=原号、原 reversed_by_id=红字号），三道幂等（原非 APPROVED/已冲销/已有红字 拒）+ 闭月抛 PeriodClosedException（须先人工 reopen 不自动重开）；REST POST /api/gl/vouchers/{docNo}/reverse + reverse_voucher HIGH HITL（常驻 53）；**统一防删验收 NoPhysicalDeleteArchitectureTest**（反射断言全 9 单据聚合+凭证/服务/仓储无 delete·remove + DocumentStatus COMPLETED 仅→REVERSED，**路线图验收「尝试删除已过账单据的所有路径都被拒」达成**）。**关键财务红线裁定（P0 修复）**：红字法「已过账集」=**APPROVED+REVERSED**（原凭证 REVERSED 后发生额由红字对调抵消，二者须都计入才净额归零；仅统计 APPROVED 会使科目余额翻转、Σ守恒掩盖）——`JdbcVoucherRepository.aggregateBalances`（试算/T01/T05）+ `FinancialStatementDao.cumulativeBalances|profitLossMovements`（资产负债表/利润表 T06）**三处一致改** `status IN (APPROVED,REVERSED)`，DRAFT/CANCELLED 仍不计。真库 VoucherReversalFlowIntegrationTest 验收；设计真源 docs/M4拆解-统一冲销机制.md。**1394 后端测试全绿**（opus 二镜头对抗校验抓出 1 P0[上述红线，已修]+2 P1[集成测审计 target_code 据实改+净额归零随 P0 通过，已修]，1 P3[校验顺序 cosmetic]未改）。**M4-T07b 业务单据红冲已完成**：采购入库/发票·销售出库/发票红冲=**原单→REVERSED + 反向全部下游 + 红字凭证**（复用 T07a VoucherService.reverse 红冲该业务单的自动凭证，不复制单据头）；共享基元——①库存按原成本反向（OutboundCommand 加 overriddenUnitCost，采购入库按原 receipt 成本出/销售出库按固化原 COGS 入回，反向幂等键 REVERSAL:原号:行号）②AP/AR 加 REVERSED 状态 + canBeReversed(settled==0 且 OPEN)/markReversed（已核销发票须先冲收付款单）③子账量回滚（Line subtract + 服务 reverse 回写守门）④账龄/一致性排除 REVERSED；各 AppService.reverse 同一 @Transactional 原子编排（任一步失败整体回滚，闭月经自动凭证红冲账期校验天然护住）；4 REST /reverse + 4 HIGH HITL 工具（常驻 53→57）；无新增迁移列。真库 BusinessDocReversalFlowIntegrationTest 验收（库存账实归位/凭证红冲/AP·AR REVERSED 不入账龄/一致性 0 ERROR/闭月拒回滚/审计）；设计真源 docs/M4拆解-统一冲销机制.md §2。**1480 后端测试全绿**（opus 二镜头抓出 1 P0+1 P1[同根财务红线：foundation 修一致性规则 4/5 排除 REVERSED 时漏改核销 rollup 规则 8/9/10，致 REVERSED 子账报 SETTLEMENT_STATUS=ERROR→业务发票红冲后 PeriodCloseService 闸门永久阻塞月末关账；已修 checkSettlementRollup 规则10 加 REVERSED⟺settled==0 分支]+1 P2[集成测 0-ERROR 断言过滤器过窄漏 rollup 键、掩盖 P0，已修]+2 P3[销售出库红冲入库成本≤1 分舍入残差，GL 精确·库存流水=余额自洽，登记后续补 InboundCommand overriddenTotalCost]）。**M4-T07c 核销反向引擎 + 收付款红冲 + 调拨/盘点红冲已完成——M4-T07 统一冲销全部收口（凭证+业务单据+收付款/核销+调拨/盘点 全单据类型红冲）**：核销反向（AR/AP.unsettle 状态回退 SETTLED→PARTIAL→OPEN + SettlementRecord 追加**负额**反向记录 Σ恒=settled[一致性 rollup 口径不变] + SettlementService.unsettleReceivable/Payable @Audited，**无 V25 迁移**）；收付款 reverse 按 paymentDocNo 反查正向核销记录→逐条 unsettle→复用 T07a 红冲现金侧凭证→原单 REVERSED（**解锁已核销发票红冲**：先冲收付款单→子账回 OPEN→canBeReversed=true→再 T07b 红冲发票）；调拨/盘点 reverse 反向库存两腿/盘盈亏（复用 overriddenUnitCost 按原成本归位）、无 GL 红冲（不出凭证）；4 reverse 端点（收付款 finance:settlement/调拨 inventory:transfer/盘点 inventory:count）+ 4 HIGH HITL 工具（常驻 57→61）；REVERSED 终态单向。真库整链验收（收付款红冲解锁发票红冲/调拨盘点账实归位/一致性 0 ERROR/再冲拒/审计）；设计真源 docs/M4拆解-统一冲销机制.md §2 T07c。**1574 后端测试全绿**（opus 二镜头 0 P0/P1；1 P2 已修[收付款 app reverse 前置状态守门主防线，非靠 unsettle 下溢兜底]；P3 已修[reverseAutoVoucher 无凭证抛账证不符异常]+登记[调拨/盘点/销售出库红冲反向库存成本≤1 分舍入残差]；workflow 撞 session limit 经 resumeFromRunId 续跑；CI 集成 job 暴露 InventoryPostingPort 加 originalUnitCost 致 TransferIntegrationTest lambda 失效已修匿名类、6 处 reverse 单测因守门加严已对齐）。**M4-T08 财务 Agent 工具已完成——M4 财务核心全部收尾**：8 个 NORMAL 只读财务查询工具（账龄×2 query_receivable_aging/payable_aging、资产负债表·利润表×2 query_balance_sheet/income_statement、试算·科目余额×2 query_trial_balance/account_balance、核销流水×2 query_receivable/payable_settlements）——纯封装已有只读服务，零新增领域逻辑/迁移/权限点，权限对齐 REST（finance:settlement/report/voucher，NORMAL 但带 requiredPermission，与 precheck_period_close 先例一致非"登录即可"），金额 BigDecimal 字符串/日期 ISO/异常映射 ToolResult.fail；常驻工具 61→69（演示 2→全量注册 71，HighRiskToolPermissionTest 基线 ≥71、HIGH 数不变 39）；全反应堆本地测试全绿（sjherp-app 926，T08 八工具单测 84 用例），只读封装无新增集成测试（底层服务已有真库覆盖）；opus 单镜头对抗校验 0 P0/P1/P2，2 P3 已修（DomainToolConfig/测试计数注释漂移校准为常驻 69、QueryReceivableAgingTool size 超限改截断至 MAX_SIZE 200 而非重置 20）；文档同步领域工具清单/权限矩阵/LlmAgent 提示词；设计真源 docs/M4拆解-财务Agent工具.md。**下一步进入 M5 生产模块**；现金流量表 P2 延后；T02 后续小批补期初/盘点/成本调整凭证；T07 遗留小批 InboundCommand/OutboundCommand overriddenTotalCost 消除红冲库存成本≤1 分残差。遗留 P2（已登记路线图 §0.5，非阻塞）：三单校验短路漏报、财务台账反向孤儿盲区（与 M4 核销同期）、一致性聚合未含 batch_id、导入空文件健壮性、报表 keyword LIKE 索引/ESCAPE 与毛利·库存总值权限敏感度（与库存查询同既有模式）；一致性闸门全库口径（可因他期 ERROR 阻塞本期关账，按账期收敛留后续优化）；重开后重结硬拒（依赖 T07 红冲）。**M3 进销存 + M4 财务核心全部完成（T01 总账→T02 业务自动凭证→T03 应收应付核销/账龄→T04 收付款单[资金闭环]→T05 月末结转关账[beta 退出标准核心]→T06 财务报表[资产负债表/利润表]→T07 统一冲销[全单据类型红冲]→T08 财务 Agent 工具）——v0.5 beta（M3+M4 财务核心）里程碑达成**；现金流量表 P2 延后。**M5 生产模块开篇——M5-T01 BOM + 工艺路线极简版已完成**：新建 com.sjherp.domain.production 两聚合 BillOfMaterials（header 父产品+版本+ArchiveStatus 启停+行[子件/用量/损耗率/单位]）+ Routing（header+工序行[序号/工序名/单位工时+预留 work_center/cost_rate 给 T06]）；**多层 BOM=单层存储+递归 explode**、**环检测双保险**（保存时 DFS 拦多层环 A→B→C 后建 C→A + explode 路径栈兜底脏数据，数据模型红线）、损耗率加成法 毛=净×(1+scrapRate)（scrapRate∈[0,1)、全程 BigDecimal）、**同产品至多一启用版本**（enable/create 同事务先停旧再启 + DB active_flag 生成列唯一索引兜底）、无业务编号（product+version 自然标识）；V25 四表 + JdbcRepository（先删后插+批量 IN 防 N+1）+ REST /api/production/boms|routings（写护新权限点 production:bom|routing[ADMIN/BOSS]、查询/explode 登录即可）+ TransactionalBom/RoutingService（app 层 @Transactional 薄委托保 create/enable 多写原子）；Agent 工具留 M5-T07。设计真源 docs/M5拆解-BOM工艺路线.md。**全反应堆本地测试全绿**（opus 单镜头对抗校验抓出 1 P0[create 先插 ENABLED 后停旧版本→真库撞 active 唯一索引，改先停后插+Fake 补约束模拟回归守门]+1 P1[生产服务多写无事务边界→加 Transactional 包装]+1 P2[costRate 请求 @NotNull 与预留可空矛盾+响应 NPE]均已修）。**M5-T02 SOP/DP 需求计划 + MRP 简化展开已完成**：DemandPlan（手工预测落表 + 销售订单需求实时聚合不复制）+ **MrpService 核心 low-level-code 逐层 netting**——父件 net=max(毛−库存,0) 作生产建议量，再用 **net**（非毛，与 explode 本质区别）×行用量×(1+损耗)展开子件毛需求；LLC 升序逐层、同层多来源合并后一次净算（菱形共用件最深层只扣一次库存）；有 active BOM→生产建议/无 BOM→采购建议（叶子）；单位归一到子件基本单位（换算缺失抛异常）、环防护、净需求负截 0、全程 BigDecimal；MrpRun/Suggestion regenerative 持久化（供 T03 转工单引用）。V26 四表 + REST（/api/production/demand-plans 写护 production:plan、/api/production/mrp/runs 触发与查询**全程受控** production:mrp[建议含经营敏感数据查询同权]）+ Transactional 包装；domain 加 assertj-core(test scope)。Agent 工具留 M5-T07。设计真源 docs/M5拆解-需求计划与MRP.md。**全反应堆本地测试全绿（1813）**，教科书 A=90/B=160/C=430 验收（opus 0 P0、1 P1[MRP 查询漏护权限点]+2 P2[补菱形 LLC/单位换算缺失测试]+2 P3 均已修）。**M5-T03 WO 生产任务单已完成（2026-06-15）**：WorkOrder 聚合根（继承 BusinessDocument，**beforeTransition 白名单收紧为 5 条**：DRAFT→APPROVED/CANCELLED、APPROVED→EXECUTING/REVERSED、EXECUTING→COMPLETED；EXECUTING/COMPLETED→REVERSED 硬拒）；`create()`（11 参手工建单）+ `createFromSuggestion()`（6 参 MRP 转单，mrpRunDocNo/sourceType 绑定）+ `restore()`（16 参回读）；`release/start/complete/cancel/reverse()` 状态方法；WorkOrderService 领域服务 + WorkOrderQuery record（productId/status/page/size）；JdbcWorkOrderRepository（V27 work_order 表含 reversal_of_id/reversed_by_id 冲销链路列，insert id 回填，update 持久化 status + 冲销链路）；从 MRP 建单按 mrpRunDocNo+productId 取 PRODUCTION 建议（netRequirement→plannedQty）；纯头无子行（领料/工序行留 T04/T05）；冲销 PLANNED 走 cancel、RELEASED 走 reverse，本批不动库存/不出凭证。REST /api/production/work-orders/**（**全程受控** production:wo ADMIN/BOSS 含查询同权）+ WorkOrderDtos + TransactionalWorkOrderService + ProductionExceptionHandler（WorkOrderNotFound→404/IllegalStateTransitionException→409/IAE·校验→400）；**全反应堆 1037 后端测试全绿**。设计真源 docs/M5拆解-工单.md。**opus 单镜头对抗校验 0 P0**（状态机白名单无漏洞，否决全部跳级/终态/投产后冲销且只收紧不放宽）；1 P1 已修（reverse() 的 reversedById 原静默丢弃[V27 缺列 + update 未写]违反兄弟单据范式与可审计红线→补列 + update 持久化）；2 P3 已修（删 DTO 死方法、WorkOrder 登记进 NoPhysicalDeleteArchitectureTest 防删守卫）。Agent 工具留 T07。下一步 M5-T04 JIT 领料与齐套。**M5-T04 JIT 领料与齐套已完成（2026-06-19）——工单首次真正消耗库存**：com.sjherp.domain.production 续建两单据聚合 **MaterialIssue（MI-）领料单**（header 工单号/领料仓/六态+行 子件/应领/实领/单位/issuedCost 过账回填；一工单可多张累加=JIT 分批+补料）+ **MaterialReturn（MR-）退料单**（独立单据非负数行，引用原领料单，**按原领料成本退回**避期间进货成本漂移）；**领料过账绝不绕过 M3 库存唯一写入口**（红线）——生产域窄端口 InventoryPostingPort→app TransactionalInventoryService 适配（领域零依赖），post 同一 @Transactional：APPROVED→EXECUTING→逐行 OutboundCommand(PRODUCTION_ISSUE，移动加权)一批 execute（库存不足整批回滚）→回填 issuedCost=totalCost().negate()→COMPLETED，幂等键 MATERIAL_ISSUE:docNo:行号；退料对称 PRODUCTION_RETURN+InboundCommand(unitCost=原领料单价)；**齐套检查只读单层不预留库存仅提示不阻断**（active BOM 直接子件需求 vs 领料仓 balanceOf→缺料清单+kitted 布尔）；**本批不调 AutoVoucherService 不出"借生产成本/贷原材料"凭证**（料工费归集与 GL 留 T06）；InventoryTxnType +PRODUCTION_ISSUE(OUT)/PRODUCTION_RETURN(IN) 零破坏扩展（InventoryService 只看 direction()）；V28 四表 + **加宽 inventory_transaction.txn_type VARCHAR(16)→(32)**（容纳 PRODUCTION_RETURN 17 字符）+ JdbcMaterialIssue/ReturnRepository + REST /api/production/material-issues|material-returns|kitting-check（类级 @PreAuthorize('production:material') 全程受控含查询，独立 KittingCheckController 避 production:wo 冲突）+ AppService(@Transactional 跨库存编排) + ProductionExceptionHandler(+404/409)；新增 production:material 权限（ADMIN/BOSS）；Agent 工具留 T07。**全反应堆本地测试全绿 1927**（agent69+domain764+infra36+app1058）；设计真源 docs/M5拆解-领料齐套.md；测试文档 docs/测试-M5-T04领料齐套.md。**CI feat/m5-t04 PR#1 三 job 全绿**（前端/后端 verify/DB 集成）——integration-db 真库捕获 3 处本地测不到的问题并修复：①TestConfig 缺 StockChecker bean ②断言列名 total_amount→cost_amount ③txn_type 列宽（详见设计文档 §9）。**自驱开发循环工程方案已落地** docs/工程-自驱开发循环.md（每任务推 feat/mx-txx 分支→PR 触发 CI+人合并门）。**opus 单镜头对抗校验 0 P0**（库存唯一入口/移动加权/原子回滚/issuedCost 符号/不出 GL/权限受控全正确）；1 P1 已修（退料超退校验完全缺失可凭空增库存→create 加防超退[退料商品须在原领料已领+本单累计退料量≤原实领量]+单测）；1 P2 已修（resolveUnitCost 对未领商品 .orElse(ZERO) 静默零成本入库稀释移动加权→改 orElseThrow+单测）；P3 已修/登记（齐套路由与 Permission 注释对齐、R8 单位换算简化登记后续与 MRP 归一对齐）。下一步 M5-T05 报工与完工入库。**M5-T05 报工与完工入库已完成（2026-06-23）——工单首次产出产成品入库**：新建 com.sjherp.domain.production.ProductionReport（PR- 单据，继承 BusinessDocument 六态，**一单合一管 工时行+完工数量+完工入库**）；**完工入库走 M3 库存唯一写入口**（复用 InventoryPostingPort 适配器不新建，新增 InventoryTxnType.PRODUCTION_IN[IN]）：post 同一 @Transactional 校验工单 EXECUTING→单条 InboundCommand(PRODUCTION_IN，幂等键)经 inventory.execute→回填 inboundCost→workOrder.recordCompletion(completedQty)+save→COMPLETED；**成本=料费占位增量模型**（本次结转料费=工单全部已过账领料 issuedCost−前序报工已结转 inbound_cost，保证 Σ入库≡Σ领料出库守恒 R1，"工费"留 T06；无料/增量≤0 拒过账 D3）；**修 JdbcWorkOrderRepository.update 补 completed_qty 列**（原漏写致回写静默丢失）；工单不自动完工/超额放行/scrapQty 记录不入库；本批不出 GL（T06）。V29 两表 + REST /api/production/reports（类级 production:report 全程受控含查询）+ 新增 production:report 权限（ADMIN/BOSS 三处同步）；Agent 工具留 T07。**全反应堆本地测试全绿 1963**（agent69+domain777+infra36+app1081）；设计真源 docs/M5拆解-报工完工入库.md，测试文档 docs/测试-M5-T05报工完工入库.md。**opus 单镜头对抗校验抓出 1 P0 已修（财务红线）**：分批完工（≥2 报工单）料费重复入账→产成品价值虚增/污染移动加权/违反 R1 守恒（根因：原多次报工单测/集成测只断言数量不断言金额掩盖）→增量模型修复+3 守恒断言固化；P1（assignInboundCost 守门）/P3（javadoc）已修，P2（≤1分残差）登记。**本任务全程走自驱开发循环工程方案**（plan→设计→sonnet 实现→构建→opus 校验→修→推 feat/m5-t05 分支→PR 触发 CI+人合并门）。下一步 M5-T06 生产成本归集与结转（全项目最难财务点，须先写 docs/生产成本核算规则.md ADR：WIP 约当产量/定额法选一）。**M5-T06 生产成本归集与结转已完成（2026-06-23）——全项目最难财务点收口**：会计政策 ADR docs/生产成本核算规则.md（用户拍板 **WIP 约当产量法**）。新建 `ProductionCostSettlement`（PC- 月末成本结转单）按工单归集 料(Σ领料 issuedCost，T05 口径读不改)/工(Σ报工工时×Routing cost_rate，无 rate 用默认费率参数)/费(工时×制造费用率参数 D1)；**约当产量法**仅工费参与（料 100% 随完工结转由 T05 守恒锁定）、单位工费=工费/(完工量+在产约当量)、尾差并入完工、除零防护；**完工工费经库存唯一入口 CostAdjustCommand（NEUTRAL 调金额不调量）追加产成品成本**，不破移动加权/库存恒等式、不动 T05 inboundCost 料成本，**增量防重复**（本次=Σ−前期已结转）保证跨期 Σ守恒；**WIP=5001 生产成本借方余额**（不设在产品 SKU）；GL（ProductionCostVoucherService 照 AutoVoucherService 幂等）料归集 借5001/贷1403、人工 借5001/贷2211、制造费用两段式(5101 月末归零)、完工结转 借1405/贷5001（科目全在 V19）；顺序硬约束 报工(T05)→成本结转(T06)→关账(M4-T05)，COST 类不参与损益结转故 5001 留 WIP。V30 三表 + REST /api/production/cost-settlements（production:cost 全程受控，ADMIN/BOSS/ACCOUNTANT 会计动作）+ ConsistencyCheckService 规则11（完工工单工费已结转 WARN 非阻塞）。**全反应堆本地测试全绿 2004**（agent69+domain800+infra36+app1099）；设计真源 docs/生产成本核算规则.md(ADR)+docs/M5拆解-生产成本归集.md，测试文档 docs/测试-M5-T06生产成本归集.md。**opus 单镜头对抗校验 0 P0/P1（可合入）**：约当法无丢分/GL 借贷平衡/T05↔T06 守恒/库存恒等式不破/增量防重复双侧/关账衔接全正确，集成测真断金额+GL Σ借=Σ贷+5001 WIP+一致性 0 ERROR+幂等+关账。2 P2：双锚点跨建单→过账口径不一致登记 R-T06-8 技术债（D5 概率低）、宽泛 IllegalStateException→409 handler 已移除（裸异常是不变式违例应 500）；3 P3：period.strip 已修、wip 前瞻 R-T06-9、多期集成测随 R-T06-8 补；主控补齐缺失的 ProductionCostSettlementApiPermissionTest（17 例）。**M5 生产模块进度 T01–T06 完成**（BOM/工艺→需求计划 MRP→工单→领料齐套→报工完工入库→成本归集结转），剩 T07 生产 Agent Tools、T08 M5 交叉校验。下一步 M5-T07。

0→1 完整计划见 **docs/产品路线图-0到1.md**（任务编号制，分配 subagent 时引用编号；该文档是计划的单一真源）。当前推进位置与已知技术债以该文档为准。
